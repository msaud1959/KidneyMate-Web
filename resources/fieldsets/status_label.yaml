title: 'Status label'
fields:
  -
    handle: build_status
    field:
      type: select
      display: Status
      default: development
      instructions: 'Only mark In prototype when the capability genuinely works in the current build.'
      options:
        prototype: 'In prototype'
        development: 'In development'
        planned: 'Planned'
